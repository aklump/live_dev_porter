# Lando Plugin

* Provides database credentials from the Lando configuration.

## Database Configuration

```yaml
environments:
  foo:
    databases:
      bar:
        plugin: lando
        service: database
```
