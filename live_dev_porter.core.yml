title: 'Live Dev Porter'
name: 'Live Dev Porter'
description: 'Simplifies the management and transfer of assets between website environments.'
version: 0.0.3
author: 'Aaron Klump <sourcecode@intheloftstudios.com>'
config_path_base: ../../..
path_to_app: .
additional_config:
  - .live_dev_porter/config.yml
  - .live_dev_porter/config.local.yml
additional_bootstrap: null
default_command: help
commands:
  config:
    help: 'Open a configuration file in $EDITOR to make changes.'
    options:
      local:
        type: boolean
        help: Use this to open config.local.yml instead of config.yml
        aliases: [ l ]
  info:
    help: 'Prints configuration info'
  export:
    help: 'Export the local database'
    options: { all_tables: { type: boolean, help: 'Include this option to ignore all table omissions by exclude files.' } }
  fetch:
    access_by_env: [ dev, staging ]
    help: 'Fetch remote assets to local.'
    options: { database: { type: boolean, help: 'Indicate the database asset.', aliases: [ d ] }, files: { type: boolean, help: 'Indicate the files asset.', aliases: [ f ] } }
  reset:
    access_by_env: [ dev, staging ]
    help: 'Reset dev assets with last fetched.'
    options: { database: { type: boolean, help: 'Indicate the database asset.', aliases: [ d ] }, files: { type: boolean, help: 'Indicate the files asset.', aliases: [ f ] } }
  pull:
    access_by_env: [ dev, staging ]
    help: 'Fetch and reset in one command.'
    options: { database: { type: boolean, help: 'Indicate the database asset.', aliases: [ d ] }, files: { type: boolean, help: 'Indicate the files asset.', aliases: [ f ] } }
  init:
    help: 'Initialize local configuration files.'
  help:
    help: 'Show this help screen.'
    options: { version: { help: 'Display the current version', type: boolean } }
  clear-cache:
    aliases: [ clearcache, cc ]
    help: 'Clear all cached data.'
environments:
  dev:
    plugin: null
    id: dev
    name: 'Local development'
    export: { path: .live_dev_porter/backups, exclude_data_from_tables_listed_in: [ data_exclusions.txt ], exclude_tables_listed_in: [ table_exclusions.txt ] }
    fetch: { path: .live_dev_porter/fetch }
  staging:
    plugin: null
    id: test
    name: Staging
  production:
    plugin: null
    id: production
    name: Production
plugin_assignments:
  fetch:
    db: PRODUCTION_PLUGIN
    files: PRODUCTION_PLUGIN
  reset:
    db: DEV_PLUGIN
    files: PRODUCTION_PLUGIN
  export:
    db: DEV_PLUGIN
  import:
    db: DEV_PLUGIN
