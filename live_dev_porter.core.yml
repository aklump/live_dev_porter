title: 'Live Dev Porter'
name: 'Live Dev Porter'
description: 'Simplifies the management and transfer of assets between website environments.'
version: 0.0.9
author: 'Aaron Klump <sourcecode@intheloftstudios.com>'
config_path_base: ../../..
path_to_app: .
additional_config:
  - .live_dev_porter/config.yml
  - .live_dev_porter/config.local.yml
additional_bootstrap: null
default_command: configtest
commands:
  remote:
    help: 'Enter the remote server shell.'
  help:
    help: 'Show this help screen.'
  info:
    help: 'Print the configuration info.'
  #  fetch:
  #    require_write_access: true
  #    help: 'Fetch the database without resetting.'
  #    options:
  #      verbose:
  #        type: boolean
  #        help: 'Output additional information.'
  #        aliases:
  #          - v
  #      database:
  #        type: boolean
  #        help: 'Indicate the database asset.'
  #        aliases:
  #          - d
  #      files:
  #        type: boolean
  #        help: 'Indicate the files asset.'
  #        aliases:
  #          - f
  #      group:
  #        type: string
  #        help: 'Limit the operation by group'
  #  reset:
  #    require_write_access: true
  #    help: 'Reset your database with the most recently pulled version.'
  #    options:
  #      verbose:
  #        type: boolean
  #        help: 'Output additional information.'
  #        aliases:
  #          - v
  #      database:
  #        type: boolean
  #        help: 'Indicate the database asset.'
  #        aliases:
  #          - d
  #      files:
  #        type: boolean
  #        help: 'Indicate the files asset.'
  #        aliases:
  #          - f
  #      group:
  #        type: string
  #        help: 'Limit the operation by group'
  pull:
    require_write_access: true
    help: 'Pull remote database and files to local.'
    options:
      verbose:
        type: boolean
        help: 'Output additional information.'
        aliases:
          - v
      database:
        type: boolean
        help: 'Only pull the database.'
        aliases:
          - d
      files:
        type: boolean
        help: 'Only pull the files.'
        aliases:
          - f
      group:
        type: string
        help: 'Limit the operation by group'
      dry-run:
        type: boolean
        help: 'Preview which files will be copied.'
  import:
    require_write_access: true
    help: 'Import a dumpfile to the local database.'
  export:
    help: 'Export the local database.'
    arguments:
      basename:
        help: Optional basename to use instead of the default, e.g., "rollback".
    options:
      id:
        type: string
        help: Indicate the database ID to export, this can be omitted when only one database is defined, or when you want the first (default) database to be used.
      workflow:
        type: string
        help: A workflow to use for export; this will be used instead of the configured default.
      all:
        type: boolean
        help: 'Include this option to ignore the tables configuration, that is to say export all tables and data.'
  db:
    help: 'Enter the local database shell.'
    aliases:
      - mysql
    arguments:
      id:
        help: Use to enter a database other than the first/default listed for the local environment.
  config:
    help: 'Open configuration in $EDITOR.'
    options:
      local:
        type: boolean
        help: 'Use this to open config.local.yml instead of config.yml.'
        aliases:
          - l
  configtest:
    help: 'Run tests to validate configuration.'
    aliases:
      - health
  init:
    help: 'Initialize local configuration files.'
    options:
      version:
        help: 'Display the current version'
        type: boolean
  clear-cache:
    aliases:
      - clearcache
      - cc
    help: 'Clear all cached data.'
plugin_assignments:
  local:
    databases: mysql
    files: default
  remote:
    databases: REMOTE_PLUGIN
    files: REMOTE_PLUGIN

  shell:
    db: mysql
    remote: REMOTE_PLUGIN
  pull:
    db: REMOTE_PLUGIN
    files: REMOTE_PLUGIN
  #  fetch:
  #    db: REMOTE_PLUGIN
  #    files: REMOTE_PLUGIN
  #  reset:
  #    db: LOCAL_PLUGIN
  #    files: default
  export:
    db: mysql
  import:
    db: mysql
#environments:
#  -
#    id: production
#    label: Production
#    write_access: false
#    plugin: default
#environment: production
